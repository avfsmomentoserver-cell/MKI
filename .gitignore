# --- MKC workspace: ignore generated/archival data and local envs ---

# Cloned source repos (synced separately, not committed)
repos/

# ChatGPT / research archive (local, large)
content-archive/

# Databases / local data
*.db
*.sqlite*
data/
logs/
backups/

# Python
__pycache__/
*.pyc
.venv/
backend/.venv/
backend/instance/

# Node
node_modules/

# Environment / secrets
.env
.env.*
!.env.example
backend/src/mkc.egg-info/

# Local scratch / session logs (may contain secrets)
impress_me_with_full_implementation_clo_.json
e2e_ingest.log
